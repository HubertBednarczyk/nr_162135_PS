#include <stdio.h>
#include <stdlib.h>
int n,k,maks;
int dzielnik(int n);

int main()
{
    printf("Podaj liczbe: \n");
    scanf("%d",&n);
    printf("%d",dzielnik(n));
    return 0;
}

int dzielnik(int n)
{
   for(k=1;k<n;k++)
   {
        if(n%k==0)
        maks=k;
    }
    return maks;
}
